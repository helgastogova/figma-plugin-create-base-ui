{
  "name": "generate-ui",
  "version": "1.0.0",
  "description": "Figma Plugin For creating base UI components for a design system",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "helgastogova@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "*"
  },
  "dependencies": {
    "watch": "^1.0.2"
  }
}
